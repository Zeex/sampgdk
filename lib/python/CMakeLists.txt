set(PYTHONPATH
  ${CMAKE_CURRENT_SOURCE_DIR}/cidl
  ${CMAKE_CURRENT_SOURCE_DIR}/ply
)

if(NOT CMAKE_HOST_SYSTEM_NAME STREQUAL Windows)
  string(REPLACE ";" ":" PYTHONPATH "${PYTHONPATH}")
endif()

sampgdk_set_property(SAMPGDK_PYTHONPATH "${PYTHONPATH}")
